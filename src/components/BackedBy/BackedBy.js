import styles from "./BackedBy.module.css"
import React,{useState,useEffect,useRef,useContext,FC,
    SetStateAction,
    Dispatch} from 'react'
 
 
const BackedBy = () => {
      return (
          <div className={styles.container}>
              
          </div>
      )
  }
  
  export default BackedBy